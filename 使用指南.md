# 115 网盘管理器 - 使用指南

## 快速开始

### 第一步：环境准备

#### 1. 安装 Python
- 下载地址：https://www.python.org/downloads/
- 版本要求：Python 3.9 或更高版本
- **重要**：安装时务必勾选 "Add Python to PATH"

#### 2. 验证安装
打开命令行（Windows: cmd/PowerShell，macOS/Linux: 终端），输入：
```bash
python --version
# 或
python3 --version
```
如果显示版本号（如 `Python 3.9.7`），说明安装成功。

### 第二步：安装依赖

在项目目录中执行：
```bash
pip install -r requirements.txt
```

或者使用 Python 3：
```bash
pip3 install -r requirements.txt
```

### 第三步：配置 Cookie

#### 获取 Cookie
1. 用 **Chrome 或 Edge** 浏览器打开 https://115.com 并登录
2. 按 **F12** 打开开发者工具
3. 切换到 **Network（网络）** 标签
4. 随便点一下页面（触发一个请求）
5. 在左侧请求列表里点击任意一条
6. 右侧找到 **Request Headers → Cookie**
7. 复制冒号后面**整行**的值
   - 格式类似：`USERSESSIONID=xxx; UID=xxx; CID=xxx; SEID=xxx; ...`

#### 保存 Cookie
在项目根目录创建 `cookie115.txt` 文件，将复制的内容粘贴进去（只保留一行）。

### 第四步：启动服务

#### Windows 用户
双击 `启动115管理器.bat` 文件。

#### macOS/Linux 用户
```bash
# 赋予执行权限（首次）
chmod +x start.sh

# 启动服务
./start.sh
```

#### 手动启动
```bash
python server.py
# 或
python3 server.py
```

### 第五步：访问界面

服务启动后，浏览器会自动打开 `http://127.0.0.1:8765`。

如果没有自动打开，手动在浏览器中输入该地址。

## 功能详解

### 1. 目录浏览

#### 基本操作
- **选择根目录**：左上角下拉菜单选择根目录
- **展开目录**：点击目录名称展开子目录
- **查看详情**：双击目录或文件，右侧弹出详情面板

#### 两种模式
- **本地数据**：浏览已扫描的目录（速度快）
- **在线浏览**：实时读取网盘目录（需要网络）

#### 批量操作
1. 勾选目录/文件（左侧复选框）
2. 底部弹出批量工具栏
3. 选择操作：
   - 📁 **移动到…**：选目标目录，一键批量移动
   - 🏷 **打标签**：给选中项打上标签
   - 🗑 **删除**：移入 115 回收站（30天可恢复）

### 2. 扫描管理

#### 开始扫描
1. 切换到「扫描管理」页签
2. 选择一个根目录
3. 点击「开始扫描」
4. 等待扫描完成（时间取决于目录数量）

#### 扫描说明
- **串行扫描**：一个一个目录扫描，避免触发风控
- **速度**：约 3-5 秒/目录
- **断点续扫**：中断后下次启动会自动继续
- **失败重试**：自动重试，最多 3 轮

#### 定时扫描
1. 在扫描管理页签中设置定时任务
2. 选择扫描时间和目标目录
3. 服务会自动在指定时间执行扫描

### 3. 自动转存

#### 转存流程
1. 切换到「自动转存」页签
2. 粘贴分享链接（每行一条）
3. 点击「解析」
4. 确认转存清单
5. 选择目标目录
6. 点击「开始转存」

#### 支持格式
- 粘贴链接：每行一条
- 上传文件：支持 txt/csv/xlsx 格式

#### 转存说明
- **间隔控制**：每条间隔 4-6 秒，防止风控
- **重复处理**：自动跳过已转存的链接
- **失效处理**：记录失效链接，便于后续处理

### 4. 搜索

#### 基本搜索
1. 切换到「搜索」页签
2. 输入关键字（≥2字）
3. 按回车或点击搜索按钮

#### 高级筛选
- **类型筛选**：全部/仅目录/仅文件
- **根目录筛选**：选择特定根目录
- **大小范围**：设置文件大小范围

#### 搜索结果
- 显示完整路径
- 支持直接打开、扫描、删除、下载

### 5. 结果与查重

#### 查重流程
1. 切换到「结果与查重」页签
2. 点击「开始分析」
3. 等待分析完成

#### 查重结果
- **精确重复**：内容完全相同
- **模糊重复**：名字相似，内容可能不同
- **重复数量**：显示重复文件数量

#### 批量删除
1. 勾选要删除的重复文件
2. 点击「批量删除」
3. 确认删除

### 6. AI 分类

#### 配置 AI
1. 创建 `ai_config.json` 文件
2. 配置 AI 提供商和 API Key
3. 示例配置：
```json
{
  "provider": "deepseek",
  "api_key": "your_api_key_here",
  "model": "deepseek-chat",
  "base_url": "https://api.deepseek.com"
}
```

#### 使用 AI 分类
1. 切换到「AI 分类」页签
2. 选择分类范围
3. 点击「试跑 20 条」核对准确率
4. 确认准确后点击「全量分析」
5. 审核 AI 建议
6. 通过的建议自动落成标签

#### 支持的 AI 提供商
- **DeepSeek**：国内优质 AI 服务
- **Kimi**：月之暗面 AI 服务
- **OpenAI 兼容接口**：支持各种 OpenAI 兼容 API

### 7. 标签管理

#### 预设标签
项目提供 21 个预设标签，分为以下类别：
- **类型**：电影、剧集、动漫、纪录片、综艺、演唱会·音乐、音乐、体育
- **分辨率**：4K、1080P、720P、480P、Remux、Web-DL、HDTV、BluRay、原盘、Encode、HEVC、H.264、10bit
- **字幕**：中字、国语、粤语、日语、英语、双语、多音轨
- **国家**：华语、日本、韩国、欧美、港台、印度
- **画质**：HDR、HDR10、HDR10+、Dolby Vision、HLG、SDR
- **音频**：DTS-HD、DTS-HD MA、TrueHD、Atmos、DTS、DTS-X、AC3、AAC、FLAC、LPCM、DSD
- **状态**：未看、在看、看完可删、待人工
- **来源**：分享转存、自购、录制

#### 手动打标
1. 勾选目录/文件
2. 点击底部工具栏「🏷 打标签」
3. 选择要打的标签
4. 点击「应用」

#### 规则打标
1. 切换到「标签」页签
2. 点击「规则试跑」预览效果
3. 确认后点击「规则打标…」执行

#### 按标签筛选
1. 在标签页签中点击任意标签
2. 显示打了这个标签的全部资源
3. 勾选后可批量移动

### 8. 备份恢复

#### 自动备份
- **启动时**：立即备份一次（基线）
- **每日凌晨 3 点**：自动备份
- **保留策略**：最近 7 个每日备份 + 4 个周备份

#### 手动备份
1. 点击右上角「💾 备份」按钮
2. 点击「创建备份」
3. 等待备份完成

#### 恢复备份
1. 在备份列表中选择要恢复的备份
2. 点击「恢复」
3. 确认恢复（恢复前会自动备份当前数据库）

## 高级功能

### 1. 局域网访问

服务启动后，同一 Wi-Fi 下的其他设备可以访问：

1. 启动时 cmd 窗口会显示「本机局域网 IP: xxx.xxx.xxx.xxx」
2. 其他设备浏览器打开 `http://这个IP:8765`

**注意**：手机上也能用，横屏效果更好。

### 2. 自定义配置

复制 `.env.example` 为 `.env`，按需修改：

```env
# 服务端口
PORT=8765

# 数据库路径
TREE_DB=115_tree.db

# 扫描间隔（秒）
SCAN_INTERVAL_MIN=3.0
SCAN_INTERVAL_MAX=5.0

# 备份设置
BACKUP_KEEP_DAILY=7
BACKUP_KEEP_WEEKLY=4
```

### 3. 日志查看

点击右上角「📜 日志」按钮，打开实时日志窗口：
- 支持按来源过滤
- 自动跟随滚动
- 支持清屏
- 窗口可拖动和拉伸

### 4. Cookie 管理

点击页面右上角 Cookie 状态区域，打开 Cookie 管理面板：
- 查看当前 Cookie 状态
- 更新 Cookie
- 验证 Cookie 有效性

## 常见问题

### Q1：双击 bat 后闪退 / 提示找不到 Python？
**A**：确认 Python 已安装并加入了 PATH。打开 cmd 输入 `python --version`，能显示版本号即可。

### Q2：Cookie 显示「失效」？
**A**：按「配置 Cookie」的步骤重新获取一次即可。

### Q3：扫描很慢？
**A**：扫描是串行的（3-5秒/目录），这是为了不触发 115 的风控。一个几千目录的根扫描大约需要几小时，但支持断点续扫——中途关掉服务，下次启动会自动续上。

### Q4：删除的文件能恢复吗？
**A**：删除操作会移入 115 回收站，30 天内可在 115 网盘的回收站里手动恢复。

### Q5：想换台电脑用？
**A**：把整个文件夹拷过去，新电脑装好 Python，双击 bat 即可。数据库、配置、凭据全在里面。

### Q6：AI 分类不准确？
**A**：
1. 先试跑 20 条核对准确率
2. 调整 AI 配置（如 temperature 参数）
3. 使用更强大的 AI 模型
4. 手动修正后重新训练

### Q7：转存失败怎么办？
**A**：
1. 检查链接是否有效
2. 检查 Cookie 是否过期
3. 检查网络连接
4. 查看日志了解具体错误

### Q8：如何提高扫描速度？
**A**：
1. 减小扫描间隔（有风控风险）
2. 只扫描需要的目录
3. 使用定时任务在空闲时间扫描
4. 避免同时进行其他操作

## 最佳实践

### 1. 首次使用建议
1. 先扫描一个中等大小的目录试试
2. 熟悉各个功能的基本操作
3. 备份当前数据库
4. 逐步添加更多目录

### 2. 日常使用建议
1. 定期扫描，保持数据库最新
2. 使用标签管理资源，便于查找
3. 定期查重，释放存储空间
4. 备份重要数据

### 3. 性能优化建议
1. 避免同时进行多个耗时操作
2. 使用定时任务在空闲时间执行
3. 定期清理不需要的数据
4. 优化数据库索引

### 4. 安全建议
1. 不要泄露 Cookie 和 API Key
2. 定期备份数据库
3. 不要在公共网络上使用
4. 及时更新软件版本

## 技术支持

### 问题反馈
如果遇到问题，请提供以下信息：
1. 操作系统版本
2. Python 版本
3. 错误信息
4. 操作步骤
5. 日志信息

### 功能建议
欢迎提出功能建议，请说明：
1. 使用场景
2. 期望功能
3. 优先级

### 贡献代码
欢迎贡献代码，请遵循：
1. 代码规范
2. 提交规范
3. 测试要求
4. 文档要求

## 更新日志

### v1.0.0
- 初始版本发布
- 支持目录浏览、扫描、转存、查重
- 支持 AI 自动分类
- 支持标签管理
- 支持备份恢复
