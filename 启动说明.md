# 115 网盘管理器 - 启动说明

## 快速启动

### Windows 用户

#### 方法一：双击启动脚本
1. 双击 `启动115管理器.bat` 文件
2. 等待自动安装依赖（首次运行）
3. 浏览器自动打开 `http://127.0.0.1:8765`

#### 方法二：命令行启动
```cmd
# 打开命令提示符（cmd）
cd 路径\到\115manager
python server.py
```

### macOS/Linux 用户

#### 方法一：使用启动脚本
```bash
# 赋予执行权限（首次）
chmod +x start.sh

# 启动服务
./start.sh
```

#### 方法二：手动启动
```bash
# 进入项目目录
cd 路径/to/115manager

# 安装依赖（首次）
pip3 install -r requirements.txt

# 启动服务
python3 server.py
```

## 启动前准备

### 1. 检查 Python 版本
```bash
python --version
# 或
python3 --version
```
要求：Python 3.9 或更高版本

### 2. 安装依赖
```bash
pip install -r requirements.txt
# 或
pip3 install -r requirements.txt
```

### 3. 配置 Cookie
1. 用 Chrome/Edge 浏览器打开 https://115.com 并登录
2. 按 F12 打开开发者工具
3. 切换到 Network 标签
4. 点击页面触发请求
5. 找到 Request Headers → Cookie
6. 复制整行 Cookie 值
7. 创建 `cookie115.txt` 文件，粘贴 Cookie 内容

## 启动后操作

### 1. 访问界面
- 浏览器自动打开 `http://127.0.0.1:8765`
- 如果没有自动打开，手动输入该地址

### 2. 检查状态
- 页面右上角显示 Cookie 状态
- 状态应为「● Cookie 正常」
- 如果显示「失效」，重新配置 Cookie

### 3. 开始使用
- 选择根目录进行扫描
- 浏览目录结构
- 使用各项功能

## 常见启动问题

### 问题1：提示找不到 Python
**错误信息**：`[错误] 未找到 Python，请先安装 Python 3.9+`

**解决方案**：
1. 安装 Python：https://www.python.org/downloads/
2. 安装时勾选 "Add Python to PATH"
3. 重启命令行窗口
4. 验证：`python --version`

### 问题2：依赖安装失败
**错误信息**：`[错误] 依赖安装失败`

**解决方案**：
1. 检查网络连接
2. 使用国内镜像源：
   ```bash
   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
   ```
3. 手动安装依赖：
   ```bash
   pip install fastapi uvicorn curl_cffi apscheduler openpyxl python-multipart pydantic aiofiles
   ```

### 问题3：端口被占用
**错误信息**：`地址已被占用` 或 `Address already in use`

**解决方案**：
1. 查找占用端口的进程：
   ```bash
   # Windows
   netstat -ano | findstr :8765
   
   # macOS/Linux
   lsof -i :8765
   ```
2. 终止进程或修改端口：
   ```bash
   # 修改端口（在 .env 文件中）
   PORT=8080
   ```

### 问题4：Cookie 配置错误
**错误信息**：`Cookie 未配置` 或 `cookie115.txt 无效`

**解决方案**：
1. 确保 `cookie115.txt` 文件存在
2. 确保文件内容为完整的 Cookie 字符串
3. 确保包含 `UID` 字段
4. 重新从浏览器复制 Cookie

### 问题5：数据库错误
**错误信息**：`数据库锁定` 或 `database is locked`

**解决方案**：
1. 关闭其他访问数据库的程序
2. 删除 `115_tree.db` 文件（会丢失数据）
3. 重新启动服务

## 启动脚本说明

### Windows 启动脚本（启动115管理器.bat）
```batch
@echo off
setlocal
title 115 网盘管理器
cd /d "%~dp0"

# 检测端口占用
netstat -ano | findstr ":8765" | findstr "LISTENING" >nul 2>nul
if not errorlevel 1 (
    echo 服务已在运行，正在打开浏览器...
    start "" "http://127.0.0.1:8765"
    timeout /t 2 >nul
    exit /b 0
)

# 查找 Python
set "PY="
where py >nul 2>nul && set "PY=py"
if not defined PY (
    where python >nul 2>nul && set "PY=python"
)
if not defined PY (
    echo [错误] 未检测到 Python，请先安装 Python 3.9 或更高版本。
    pause
    exit /b 1
)

# 检查依赖
"%PY%" -c "import fastapi,uvicorn,curl_cffi,apscheduler,openpyxl,multipart" >nul 2>nul
if errorlevel 1 (
    echo 首次运行，正在安装依赖（约1分钟）...
    "%PY%" -m pip install -r "..\requirements.txt"
    if errorlevel 1 (
        echo [错误] 依赖安装失败
        pause
        exit /b 1
    )
)

# 启动服务
echo 启动中...
echo 访问地址: http://127.0.0.1:8765
start "" /b powershell -NoProfile -WindowStyle Hidden -Command "for($i=0;$i -lt 60;$i++){try{$c=New-Object Net.Sockets.TcpClient('127.0.0.1',8765);$c.Close();Start-Process 'http://127.0.0.1:8765';break}catch{Start-Sleep -Milliseconds 500}}"
"%PY%" server.py
```

### Linux/macOS 启动脚本（start.sh）
```bash
#!/bin/bash

echo "=========================================="
echo "           115 网盘管理器"
echo "=========================================="

# 检查 Python
if ! command -v python3 &> /dev/null; then
    if ! command -v python &> /dev/null; then
        echo "[错误] 未找到 Python，请先安装 Python 3.9+"
        exit 1
    else
        PY="python"
    fi
else
    PY="python3"
fi

echo "使用 Python: $($PY --version)"

# 检查依赖
if ! $PY -c "import fastapi" &> /dev/null; then
    echo "首次运行，正在安装依赖..."
    $PY -m pip install -r requirements.txt
    if [ $? -ne 0 ]; then
        echo "[错误] 依赖安装失败"
        exit 1
    fi
fi

# 获取本机 IP
if command -v ip &> /dev/null; then
    LOCAL_IP=$(ip route get 1 | awk '{print $7; exit}')
elif command -v ifconfig &> /dev/null; then
    LOCAL_IP=$(ifconfig | grep -Eo 'inet (addr:)?([0-9]*\.){3}[0-9]*' | grep -Eo '([0-9]*\.){3}[0-9]*' | grep -v '127.0.0.1' | head -1)
else
    LOCAL_IP="未知"
fi

echo "本机局域网 IP: $LOCAL_IP"
echo "访问地址: http://127.0.0.1:8765"

# 启动浏览器（后台）
if command -v xdg-open &> /dev/null; then
    (sleep 3 && xdg-open http://127.0.0.1:8765) &
elif command -v open &> /dev/null; then
    (sleep 3 && open http://127.0.0.1:8765) &
fi

# 启动服务
$PY server.py
```

## 手动启动步骤

### 1. 进入项目目录
```bash
cd 路径/to/115manager
```

### 2. 检查 Python
```bash
python --version
# 或
python3 --version
```

### 3. 安装依赖
```bash
pip install -r requirements.txt
# 或
pip3 install -r requirements.txt
```

### 4. 配置 Cookie
创建 `cookie115.txt` 文件，内容为 115 网盘的 Cookie。

### 5. 启动服务
```bash
python server.py
# 或
python3 server.py
```

### 6. 访问界面
打开浏览器，访问 `http://127.0.0.1:8765`

## 启动后配置

### 1. 检查 Cookie 状态
- 页面右上角显示 Cookie 状态
- 点击可打开 Cookie 管理面板
- 状态应为「● Cookie 正常」

### 2. 配置 AI（可选）
创建 `ai_config.json` 文件：
```json
{
  "provider": "deepseek",
  "api_key": "your_api_key_here",
  "model": "deepseek-chat",
  "base_url": "https://api.deepseek.com"
}
```

### 3. 自定义配置（可选）
复制 `.env.example` 为 `.env`，按需修改：
```env
PORT=8765
HOST=0.0.0.0
TREE_DB=115_tree.db
COOKIE_FILE=cookie115.txt
AI_CONFIG=ai_config.json
BACKUP_DIR=backups
BACKUP_KEEP_DAILY=7
BACKUP_KEEP_WEEKLY=4
```

## 启动日志

### 成功启动日志
```
[系统] 115 网盘管理器启动中...
[系统] 访问地址: http://127.0.0.1:8765
[系统] 本机局域网 IP: 192.168.1.100
[系统] Cookie 状态: 正常
[系统] 数据库连接成功
[系统] 启动完成
```

### 错误日志示例
```
[错误] Cookie 未配置: 请在 Cookie 管理中粘贴您的 115 网盘 Cookie
[错误] 数据库连接失败: database is locked
[错误] 端口 8765 已被占用
```

## 停止服务

### Windows
- 关闭命令行窗口
- 或按 `Ctrl+C`

### macOS/Linux
- 按 `Ctrl+C`
- 或关闭终端窗口

## 重启服务

### Windows
1. 关闭命令行窗口
2. 重新双击 `启动115管理器.bat`

### macOS/Linux
1. 按 `Ctrl+C` 停止服务
2. 重新运行 `./start.sh`

## 后台运行

### 使用 nohup（macOS/Linux）
```bash
nohup python3 server.py > server.log 2>&1 &
```

### 使用 screen（macOS/Linux）
```bash
# 创建会话
screen -S 115manager

# 启动服务
python3 server.py

# 分离会话：按 Ctrl+A，然后按 D

# 重新连接
screen -r 115manager
```

### 使用 systemd（Linux）
创建服务文件 `/etc/systemd/system/115manager.service`：
```ini
[Unit]
Description=115 网盘管理器
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/115manager
ExecStart=/usr/bin/python3 server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用并启动服务：
```bash
sudo systemctl enable 115manager
sudo systemctl start 115manager
```

## 启动性能优化

### 1. 使用虚拟环境
```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt

# 启动服务
python server.py
```

### 2. 使用国内镜像源
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3. 预编译 Python 文件
```bash
python -m compileall .
```

### 4. 使用 Gunicorn（生产环境）
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:8765 server:app
```

## 启动监控

### 1. 查看日志
```bash
# 实时查看日志
tail -f server.log

# 查看错误日志
grep "错误" server.log
```

### 2. 检查进程
```bash
# 查看进程
ps aux | grep server.py

# 查看端口
netstat -tlnp | grep 8765
```

### 3. 检查资源使用
```bash
# 查看 CPU 和内存使用
top -p $(pgrep -f server.py)

# 查看磁盘使用
du -sh *
```

## 故障排除

### 1. 启动失败
- 检查 Python 版本
- 检查依赖安装
- 检查端口占用
- 查看错误日志

### 2. 访问失败
- 检查服务是否启动
- 检查防火墙设置
- 检查网络连接
- 检查端口映射

### 3. 功能异常
- 检查 Cookie 配置
- 检查数据库状态
- 检查日志信息
- 重启服务

## 安全建议

### 1. 网络安全
- 不要在公共网络上暴露服务
- 使用防火墙限制访问
- 定期更新依赖

### 2. 数据安全
- 定期备份数据库
- 不要泄露 Cookie
- 使用强密码

### 3. 系统安全
- 定期更新系统
- 使用安全组策略
- 监控异常访问
