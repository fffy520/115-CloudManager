# 115 网盘管理器 - 项目总结

## 项目概述

115 网盘管理器是一个功能强大的本地 115 网盘管理工具，旨在帮助用户高效整理和管理 115 网盘中的海量资源。

## 核心功能

### 1. 目录浏览与管理
- 完整浏览 115 网盘目录结构
- 支持无限层级展开
- 显示目录大小、文件数量等关键信息
- 支持在线浏览和本地数据两种模式

### 2. 智能扫描系统
- 将云端目录信息同步到本地 SQLite 数据库
- 串行扫描（3-5秒/目录），有效避免触发 115 风控
- 支持断点续扫，中断后可自动继续
- 失败自动重试，最多 3 轮
- 支持定时扫描任务

### 3. 批量转存功能
- 支持粘贴分享链接（每行一条）
- 支持上传 txt/csv/xlsx 文件批量导入
- 每条间隔 4-6 秒，防止风控
- 自动处理重复和失效链接

### 4. 查重清理系统
- 自动分析全库，识别重复资源
- 精确重复（内容完全相同）和模糊重复分开显示
- 支持一键批量删除多余副本
- 智能保留最佳版本

### 5. AI 自动分类
- 集成多种 AI 提供商（DeepSeek/Kimi/OpenAI）
- 为每个目录智能识别「类型」和「载体」
- 支持试跑验证准确率
- 审核通过的建议自动落成标签

### 6. 标签管理系统
- 21 个预设标签（类型/属性/状态/来源）
- 支持手动打标、规则打标、AI 落标
- 按标签筛选和批量移动
- 规则引擎支持正则表达式自动打标

### 7. 备份恢复机制
- 自动备份数据库和 Cookie
- 每日凌晨 3 点自动备份
- 保留最近 7 个每日备份 + 4 个周备份
- 支持手动备份和恢复

## 技术架构

### 后端技术栈
- **Python 3.9+**：主要编程语言
- **FastAPI**：高性能 Web 框架
- **SQLite**：轻量级本地数据库
- **curl_cffi**：模拟 Chrome TLS 指纹，绕过 WAF
- **APScheduler**：定时任务调度

### 前端技术栈
- **纯 HTML/CSS/JavaScript**：无框架依赖
- **响应式设计**：支持桌面和移动端
- **实时日志**：WebSocket 推送日志

### 数据存储
- **115_tree.db**：目录树数据库
- **cookie115.txt**：115 登录凭据
- **ai_config.json**：AI 接口配置
- **backups/**：备份目录

## 项目结构

```
115manager/
├── server.py           # 主服务（FastAPI 应用）
├── api115.py           # 115 网盘 API 客户端
├── config.py           # 配置管理（环境变量/.env）
├── scanner.py          # 目录扫描管理器
├── transfer_worker.py  # 转存工作线程
├── ai_worker.py        # AI 分类工作线程
├── tags.py             # 标签系统
├── tag_rules.py        # 标签规则引擎
├── dedup.py            # 查重功能
├── logbus.py           # 日志系统
├── tmdb.py             # TMDB API 集成（可选）
├── static/
│   ├── index.html      # 前端页面
│   ├── app.js          # 前端逻辑（161KB）
│   └── style.css       # 样式表
├── requirements.txt    # Python 依赖
├── start.sh            # Linux/macOS 启动脚本
├── 启动115管理器.bat    # Windows 启动脚本
├── .env.example        # 环境变量配置模板
├── ai_config.example.json # AI 配置模板
├── README.md           # 项目文档
└── 项目结构图.md       # 架构图
```

## 核心模块说明

### 1. server.py（主服务）
- **功能**：FastAPI 应用，提供所有 REST API
- **端口**：8765
- **特性**：
  - 静态文件服务
  - 定时任务调度
  - 数据库备份
  - 实时日志推送

### 2. api115.py（API 客户端）
- **功能**：封装 115 网盘 API
- **特性**：
  - 使用 curl_cffi 模拟 Chrome TLS 指纹
  - 自动检测 WAF 拦截
  - 冷却重试机制
  - 统一错误处理

### 3. scanner.py（扫描管理）
- **功能**：目录扫描和同步
- **特性**：
  - 串行扫描，避免风控
  - 断点续扫
  - 定时任务支持
  - 进度跟踪

### 4. transfer_worker.py（转存工作线程）
- **功能**：处理分享链接转存
- **特性**：
  - 批量处理
  - 间隔控制
  - 重复检测
  - 失败重试

### 5. ai_worker.py（AI 分类）
- **功能**：调用 AI 进行资源分类
- **特性**：
  - 多提供商支持
  - 批量处理
  - 结果审核
  - 自动落标

### 6. tags.py（标签系统）
- **功能**：管理标签和关联
- **特性**：
  - 预设标签库
  - 多来源支持
  - 批量操作
  - 筛选查询

### 7. tag_rules.py（规则引擎）
- **功能**：按名字正则自动打标
- **特性**：
  - 正则表达式匹配
  - 批量处理
  - 规则管理
  - 结果预览

## 使用流程

### 首次使用
1. 安装 Python 3.9+
2. 安装依赖：`pip install -r requirements.txt`
3. 配置 Cookie：创建 `cookie115.txt`
4. 启动服务：`python server.py` 或双击 bat 文件
5. 访问界面：`http://127.0.0.1:8765`

### 日常使用
1. **浏览目录**：选择根目录，展开浏览
2. **扫描目录**：选择目录，开始扫描
3. **转存资源**：粘贴分享链接，选择目标目录
4. **管理标签**：给资源打标签，按标签筛选
5. **查重清理**：找出重复资源，批量删除
6. **AI 分类**：使用 AI 自动识别资源类型

## 安全机制

### 1. 网络安全
- 使用 curl_cffi 模拟真实浏览器
- 检测 WAF 拦截并自动冷却
- 请求间隔控制，避免触发风控

### 2. 数据安全
- Cookie 本地存储，不上传第三方
- 数据库本地 SQLite，隐私可控
- 定期备份，防止数据丢失

### 3. 操作安全
- 删除操作移入回收站，30天可恢复
- 批量操作前确认
- 日志记录所有操作

## 扩展性

### 1. 添加新功能
- 在 server.py 中添加新的 API 端点
- 创建新的业务模块
- 更新前端界面

### 2. 集成新 AI 提供商
- 修改 ai_worker.py 中的 API 调用逻辑
- 添加新的提供商配置
- 支持自定义模型

### 3. 自定义标签规则
- 修改 tag_rules.py 中的正则表达式
- 添加新的规则模式
- 支持复杂匹配逻辑

### 4. 集成外部服务
- TMDB API 集成（已实现）
- 其他元数据服务
- 自定义数据源

## 性能优化

### 1. 扫描优化
- 串行扫描，避免并发请求
- 断点续扫，减少重复工作
- 缓存机制，提高响应速度

### 2. 数据库优化
- SQLite WAL 模式
- 索引优化
- 批量操作

### 3. 前端优化
- 懒加载
- 虚拟滚动
- 缓存策略

## 部署方案

### 1. 本地部署
- 单机运行，适合个人使用
- 无需服务器，开箱即用
- 局域网访问，多设备共享

### 2. 家庭 NAS 部署
- 部署到 NAS 设备
- 24 小时运行
- 远程访问（需配置端口映射）

### 3. 云服务器部署
- 部署到云服务器
- 公网访问
- 需注意安全配置

## 未来规划

### 短期目标
- 优化 AI 分类准确率
- 增加更多标签规则
- 完善错误处理

### 中期目标
- 支持更多网盘平台
- 添加移动端 APP
- 集成更多元数据服务

### 长期目标
- 构建资源社区
- 智能推荐系统
- 自动化工作流

## 总结

115 网盘管理器是一个功能完善、架构清晰的本地网盘管理工具。它通过智能扫描、批量转存、AI 分类、标签管理等功能，帮助用户高效整理和管理 115 网盘中的海量资源。

项目采用 Python + FastAPI 技术栈，具有良好的扩展性和可维护性。通过本地部署，用户可以完全掌控自己的数据，无需担心隐私泄露。

无论是个人用户还是家庭用户，都可以通过这个工具轻松管理自己的 115 网盘资源，提高资源整理效率，释放存储空间。
