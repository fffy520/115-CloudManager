# 115 网盘管理器 - 项目结构图

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    115 网盘管理器                            │
├─────────────────────────────────────────────────────────────┤
│  前端层 (Browser)                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ index.html  │  │   app.js    │  │ style.css   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
├─────────────────────────────────────────────────────────────┤
│  API 层 (FastAPI)                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  server.py                          │   │
│  │  - REST API 端点                                    │   │
│  │  - 静态文件服务                                      │   │
│  │  - 定时任务调度                                      │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  业务层 (Python Modules)                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ scanner.py  │  │transfer_    │  │ ai_worker.py│         │
│  │ 目录扫描    │  │worker.py    │  │ AI 分类     │         │
│  └─────────────┘  │转存工作线程 │  └─────────────┘         │
│                   └─────────────┘                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  tags.py    │  │ tag_rules.py│  │   dedup.py  │         │
│  │ 标签系统    │  │ 规则引擎    │  │ 查重功能    │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
├─────────────────────────────────────────────────────────────┤
│  基础层 (Infrastructure)                                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ api115.py   │  │ config.py   │  │  logbus.py  │         │
│  │ 115 API     │  │ 配置管理    │  │ 日志系统    │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
├─────────────────────────────────────────────────────────────┤
│  数据层 (Storage)                                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ 115_tree.db │  │cookie115.txt│  │ai_config.json│        │
│  │ SQLite 数据库│  │ Cookie 凭据 │  │ AI 配置     │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

## 文件依赖关系

```
server.py (主入口)
    ├── api115.py (115 网盘 API)
    │   └── config.py
    ├── scanner.py (目录扫描)
    │   ├── api115.py
    │   ├── config.py
    │   └── logbus.py
    ├── transfer_worker.py (转存工作线程)
    │   ├── api115.py
    │   ├── scanner.py
    │   ├── config.py
    │   └── logbus.py
    ├── ai_worker.py (AI 分类)
    │   ├── config.py
    │   └── logbus.py
    ├── tags.py (标签系统)
    │   ├── config.py
    │   └── logbus.py
    ├── tag_rules.py (规则引擎)
    │   ├── config.py
    │   └── logbus.py
    ├── dedup.py (查重功能)
    ├── logbus.py (日志系统)
    └── config.py (配置管理)
```

## 数据流

### 1. 扫描流程
```
用户选择根目录
    ↓
server.py 提交扫描任务
    ↓
scanner.py 开始扫描
    ↓
api115.py 调用 115 API
    ↓
解析目录结构
    ↓
写入 115_tree.db
    ↓
logbus.py 记录日志
```

### 2. 转存流程
```
用户粘贴分享链接
    ↓
server.py 解析链接
    ↓
transfer_worker.py 开始转存
    ↓
api115.py 调用转存 API
    ↓
写入 115_tree.db
    ↓
logbus.py 记录日志
```

### 3. AI 分类流程
```
用户选择范围
    ↓
server.py 提交 AI 任务
    ↓
ai_worker.py 开始分类
    ↓
调用外部 AI API
    ↓
解析 AI 响应
    ↓
写入 tags.py
    ↓
logbus.py 记录日志
```

## API 端点

### 目录浏览
- `GET /api/tree` - 获取目录树
- `GET /api/node/{cid}` - 获取节点详情
- `POST /api/scan` - 提交扫描任务

### 转存
- `POST /api/transfer` - 提交转存任务
- `GET /api/transfer/status` - 获取转存状态

### AI 分类
- `POST /api/ai/classify` - 提交 AI 分类任务
- `GET /api/ai/status` - 获取 AI 分类状态

### 标签
- `GET /api/tags` - 获取所有标签
- `POST /api/tags` - 创建标签
- `POST /api/tags/apply` - 应用标签

### 查重
- `GET /api/duplicates` - 获取重复资源
- `POST /api/duplicates/delete` - 删除重复资源

### 备份
- `GET /api/backups` - 获取备份列表
- `POST /api/backups` - 创建备份
- `POST /api/backups/restore` - 恢复备份

## 数据库结构

### tree_nodes 表（目录树）
```sql
CREATE TABLE tree_nodes (
    cid TEXT PRIMARY KEY,
    parent_cid TEXT,
    name TEXT,
    size INTEGER,
    is_dir INTEGER,
    created_at TEXT,
    updated_at TEXT
);
```

### tags 表（标签）
```sql
CREATE TABLE tags (
    id INTEGER PRIMARY KEY,
    name TEXT,
    category TEXT,
    color TEXT
);
```

### node_tags 表（节点标签关联）
```sql
CREATE TABLE node_tags (
    node_cid TEXT,
    tag_id INTEGER,
    source TEXT,
    created_at TEXT,
    PRIMARY KEY (node_cid, tag_id)
);
```

### schedules 表（定时任务）
```sql
CREATE TABLE schedules (
    id INTEGER PRIMARY KEY,
    target_cid TEXT,
    target_name TEXT,
    hour INTEGER,
    minute INTEGER,
    enabled INTEGER,
    last_run TEXT
);
```

## 配置项

### 环境变量
- `PORT` - 服务端口（默认 8765）
- `HOST` - 服务地址（默认 0.0.0.0）
- `TREE_DB` - 数据库路径
- `COOKIE_FILE` - Cookie 文件路径
- `AI_CONFIG` - AI 配置文件路径
- `BACKUP_DIR` - 备份目录
- `BACKUP_KEEP_DAILY` - 每日备份数量（默认 7）
- `BACKUP_KEEP_WEEKLY` - 每周备份数量（默认 4）
- `SCAN_INTERVAL_MIN` - 扫描最小间隔（默认 3.0）
- `SCAN_INTERVAL_MAX` - 扫描最大间隔（默认 5.0）
- `SCAN_RETRY_WAIT` - 扫描重试等待时间（默认 300）
- `SCAN_RETRY_MAX` - 扫描最大重试次数（默认 3）
- `AI_BATCH_SIZE` - AI 批处理大小（默认 10）
- `AI_CALL_DELAY_MIN` - AI 调用最小延迟（默认 1.0）
- `AI_CALL_DELAY_MAX` - AI 调用最大延迟（默认 2.0）

## 安全机制

### 1. WAF 绕过
- 使用 curl_cffi 模拟 Chrome TLS 指纹
- 检测 WAF 拦截并自动冷却重试

### 2. 防风控
- 扫描串行执行（3-5秒/目录）
- 转存间隔 4-6 秒
- AI 调用延迟 1-2 秒

### 3. 数据安全
- Cookie 本地存储，不上传
- 数据库本地 SQLite
- 备份机制保护数据

## 扩展点

### 1. 添加新功能
- 在 server.py 中添加新的 API 端点
- 创建新的业务模块
- 更新前端界面

### 2. 添加 AI 提供商
- 修改 ai_worker.py 中的 API 调用逻辑
- 添加新的提供商配置

### 3. 自定义标签规则
- 修改 tag_rules.py 中的正则表达式
- 添加新的规则模式

### 4. 集成外部服务
- 添加 TMDB API 集成（tmdb.py）
- 添加其他元数据服务
